# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Environment secrets (never commit — see .env.example for required keys)
.env
.env.*
!.env.example

# bun is the authoritative package manager (see bunfig.toml); don't
# let a stray `npm install` reintroduce a second, drifting lockfile
package-lock.json
yarn.lock
pnpm-lock.yaml

# Supabase CLI cache (environment-specific, never commit)
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
coverage/
.claude/
.zcode/
# Test coverage
coverage/

# Local AI tools
.claude/
.zcode/

# Environment
.env
.env.local

# Python cache
__pycache__/
*.pyc

# Training outputs
training/artefacts/