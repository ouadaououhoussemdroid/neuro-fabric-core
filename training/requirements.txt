# Strictly pinned, reproducible training environment.
# Verified for Python 3.10–3.12 (cp312 wheels confirmed on PyPI for all
# compiled packages; pure-Python packages use py3 wheels).
#
# Resolution rules locked in by this set:
#   • braindecode 1.1.0 uses the NEW `BNCI2014_001` MOABB name.
#   • moabb 1.4.0 still exports `BNCI2014_001` and requires numpy>=2.
#   • braindecode 1.1.0 requires mne>=1.10, so mne 1.7.x cannot be used.
#   • torch and torchaudio must share the same minor (2.5.1) — otherwise
#     torchaudio's prebuilt CUDA extension fails to load (libcudart mismatch).
#   • numpy 2.x is required by moabb 1.4 and is supported by torch 2.5.
#   • onnxsim 0.6.5 and onnxoptimizer 0.4.2 provide cp312-abi3 stable-ABI
#     wheels; the export script (scripts/export_braindecode_eegconformer.py)
#     catches ImportError/Exception if either is absent.

# --- Deep learning (torch + torchaudio MUST match minor) ---
torch==2.5.1
torchaudio==2.5.1

# --- EEG stack (compatible triple) ---
braindecode==1.1.0
moabb==1.4.0
mne==1.10.1

# --- Numerics (NumPy 2.x required by moabb 1.4) ---
numpy==2.0.2
scipy==1.14.1
scikit-learn==1.5.2
pandas==2.2.3

# --- ONNX export & parity check ---
onnx==1.17.0
onnxruntime==1.20.1

# --- T-023: ONNX graph simplification & optimization ---
# 0.6.5 provides cp312-abi3 wheel (was 0.4.17, sdist-only on 3.12)
onnxsim==0.6.5
# 0.4.2 is the latest published version with cp312-abi3 wheel
# (requirements previously listed 0.9.3, which does not exist on PyPI)
onnxoptimizer==0.4.2

# --- Misc ---
pyyaml==6.0.2
tqdm==4.66.5

# --- T-021: Experiment tracking (optional; train.py degrades gracefully) ---
mlflow==2.17.0
